<h3>Tag Clouds</h3>

<p><strong>Tag Clouds management is under development.</strong></p>

<p>Original plugin settings are in the main Tags tab.</p>

<% if defined?(@tag_clouds) && @tag_clouds.any? %>
  <!-- таблица -->
<% else %>
  <p>No tag clouds yet.</p>
<% end %>

<% if defined?(@project) && @project %>
  <%= link_to 'New Tag Cloud', new_project_tag_cloud_path(@project), class: 'icon icon-add' %>
<% end %>